name: uas_mobile_lanjut
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# 1. SEMUA DEPENDENCIES UTAMA DISATUKAN DI SINI
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  get_it: ^9.2.1
  go_router: ^17.2.3
  dio: ^5.9.2
  logger: ^2.7.0
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5
  lottie: ^3.3.3

# 2. SEMUA DEV_DEPENDENCIES (UNTUK TESTING & GENERATOR) DISATUKAN DI SINI
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  isar_generator: ^3.1.0+1
  mocktail: ^1.0.5

flutter:
  uses-material-design: true