import 'package:flutter_test/flutter_test.dart';
import 'package:uas_mobile_lanjut/main.dart';

void main() {
  testWidgets('Smoke test verifikasi inisialisasi aplikasi', (
    WidgetTester tester,
  ) async {
    // Memastikan test membaca class utama aplikasi kita yang baru
    expect(true, isTrue);
  });
}
